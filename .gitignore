# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.production

# Keypair files (never commit)
phantom-keypair.json
*-keypair.json
*.id.json

# CI download / local deploy (never commit)
artifacts/

# Anchor build artifacts (keep IDL for TS client)
.solana-deploy-keypair.json
target/*
!target/idl/
!target/idl/**

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
dumped-*.so
*-fixed.so
