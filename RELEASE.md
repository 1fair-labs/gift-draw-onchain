# Release policy

This mirror is tagged **only** when `gift_draw_registry` is deployed or upgraded on Solana.

| Tag | Source commit (private app repo) | Network | Notes |
|-----|----------------------------------|---------|--------|
| `v1.0.0-devnet` | `c416b90` | devnet | Initial public mirror; program `FZzo6eBAu9qzoNWNAHvw3qjgT6J89fZeAq9xUXjiyPed` |

Do not expect frequent updates. If the tag does not change for a long time, the on-chain program bytecode is unchanged — that is intentional.
